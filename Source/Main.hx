import mini.MiniApplication;

class Main extends MiniApplication {
    public function new() {
        startGame(new Thriller());
        super();
    }

    override function init() {
        
    }
}